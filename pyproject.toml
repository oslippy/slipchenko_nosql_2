[project]
name = "slipchenko-nosql-2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pinecone-client>=6.0.0",
    "sentence-transformers>=3.0.0",
    "torch<2.3; sys_platform == 'darwin' and platform_machine == 'x86_64'",
    "transformers<4.45; sys_platform == 'darwin' and platform_machine == 'x86_64'",
    "sentence-transformers<3.1; sys_platform == 'darwin' and platform_machine == 'x86_64'",
    "numpy<2; sys_platform == 'darwin' and platform_machine == 'x86_64'",
    "pandas>=2.3.3",
    "tqdm>=4.67.3",
    "rank-bm25>=0.2.2",
    "kaggle>=2.2.0",
    "pyarrow>=24.0.0",
    "python-dotenv>=1.2.2",
]
